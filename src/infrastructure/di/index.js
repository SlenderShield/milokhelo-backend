/**
 * Dependency Injection Module
 * Entry point for DI container
 */
const { DIContainer, getContainer, createContainer } = require('./container');

module.exports = { DIContainer, getContainer, createContainer };
